# Seed payloads

Snapshot rows reference files under this directory (e.g. `seed-payloads/listing_<uuid>.json`).
The seed script does not create these files; add real payloads here for local testing if needed.
